import util.metaProperty;
import aaz.libhpsocket.ssl.component.properties.tcpPullAgent;

var _dll = aaz.libhpsocket._dll;

namespace aaz.libhpsocket.ssl.component;
class tcpPullAgent{
	ctor(listener, pSocket){
		if(pSocket){
			this.pSocket = pSocket;
		}
		elseif(listener){
			this.pSocket = _dll.Create_HP_SSLPullAgentP(listener);
			..table.gc( this, "destroy" );
		}
		else {
			error( "请指定参数1 pSocket 或参数2 pListener", 2);
		}
	}
	
	destroy = function(){
		if(this.pSocket){
			_dll.Destroy_HP_SSLPullAgent(this.pSocket);
			this.pSocket = null;
		}
	}
	
	@_metaProperty;
}
tcpPullAgent._metaProperty = ..util.metaProperty(..aaz.libhpsocket.ssl.component.properties.tcpPullAgent);
