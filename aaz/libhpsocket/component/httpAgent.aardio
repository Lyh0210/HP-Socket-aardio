import util.metaProperty;
import aaz.libhpsocket.component.properties.httpAgent;

var _dll = aaz.libhpsocket._dll;

namespace aaz.libhpsocket.component;
class httpAgent{
	ctor(listener, pSocket){
		if(pSocket){
			this.pSocket = pSocket;
		}
		elseif(listener){
			this.pSocket = _dll.Create_HP_HttpAgentP(listener);
			..table.gc(this, "destroy");
		}
		else {
			error("请指定参数@1 pSocket 或参数@2 pListener", 2);
		}
	}
	
	destroy = function(){
		if(this.pSocket){
			_dll.Destroy_HP_HttpAgent(this.pSocket);
			this.pSocket = null;
		}
	}
	
	@_metaProperty;
}

httpAgent._metaProperty = ..util.metaProperty(..aaz.libhpsocket.component.properties.httpAgent);